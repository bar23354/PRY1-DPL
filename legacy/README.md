# Legacy

Esta carpeta conserva el estado previo del proyecto para referencia durante la migración.

## Estructura

- `python/`: entrypoints históricos del proyecto.
- `web/`: baseline visual HTML estático usado para comparación 1:1.

## Estado

- No se elimina nada de aquí hasta validar paridad funcional y visual del refactor grande.
- El contenido de `legacy/` es referencia histórica y compatibilidad transicional.
- La nueva estructura activa del proyecto se está preparando en `backend/`, `frontend/`, `fixtures/`, `e2e/` y `artifacts/`.
